%h1 Listing users

%table
  %tr
    %th First name
    %th Last name
    %th

  - @users.each do |user|
    %tr
      %td= user.first_name
      %td= user.last_name
      %td= link_to 'Show', user_profile_path(user)
