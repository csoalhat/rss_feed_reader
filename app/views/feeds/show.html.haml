%p#notice= notice
%p
  %b Title:
  %h1
    = @feed.title
    (
    = link_to @feed.url
    )

  - @feed.latest_articles.each do |article|
    %h2= link_to article.title,  article.url, :target => "_blank"
    %br
      = raw article.content
    %br

= link_to 'Edit', edit_feed_path(@feed)
\|
= link_to 'Back', feeds_path
